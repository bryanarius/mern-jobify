import type { Container, Result } from 'postcss';
import type { Model } from '@csstools/postcss-cascade-layers/dist/model';
import { pluginOptions } from '@csstools/postcss-cascade-layers/dist/options';
export declare function recordLayerOrder(root: Container, model: Model, { result, options }: {
    result: Result;
    options: pluginOptions;
}): void;
